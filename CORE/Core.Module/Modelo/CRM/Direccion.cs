using DevExpress.Persistent.Base;

namespace FDIT.Core.CRM
{
    public enum Direccion
    {
        [ ImageName( "inbox-download" ) ]
        Entrada = 1,

        [ ImageName( "inbox-upload" ) ]
        Salida,
    }
}